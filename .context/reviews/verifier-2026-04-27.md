# Verifier — Evidence-Based Correctness Check

**Reviewer**: verifier  
**Date**: 2026-04-27

---

## Finding 1: `interpolateAlongTrack` — bearing fallback for identical points is correct

**File**: `src/lib/interpolate.ts:152-162`  
**Severity**: Low (informational)  
**Confidence**: High  

When `a` and `b` are identical, the function walks backward for a meaningful bearing. For an entirely degenerate track, bearing defaults to 0 (north). Verified correct.

## Finding 2: `normalizeScenes` — silent adjustment is paired with UI warnings

**File**: `src/lib/camera.ts:19-43`  
**Severity**: Low (informational)  
**Confidence**: High  

Scene range normalization is lossless. The scene editor shows overlap warnings. Verified: behavior matches architecture doc.

## Finding 3: `useExportController` — `resetSize` in finally block is correct

**File**: `src/lib/useExportController.ts:276-293`  
**Severity**: Low (informational)  
**Confidence**: High  

The cleanup is correct. The `try/catch` around `resetSize()` is defensive but appropriate.

## Finding 4: `buildFitBounds` — degenerate bounds padding uses fixed 0.1 degree offset

**File**: `src/components/MapView.tsx:219-227`  
**Severity**: Low  
**Confidence**: High  

At the equator, 0.1 degrees is ~11km. At high latitudes the longitude span could be larger. Acceptable for the degenerate edge case.

## Finding 5: Export filename sanitization fallback works correctly

**File**: `src/lib/videoEncoder.ts:180-186`  
**Severity**: Low (informational)  
**Confidence**: High  

If all characters are stripped, the `|| 'Journey'` fallback is used. Verified correct.

## Finding 6: `checkJsonDepth` — unterminated strings may cause incorrect depth count

**File**: `src/lib/parser.ts:511-528`  
**Severity**: Low  
**Confidence**: Medium  

If the input has an unterminated string, `{`/`}` inside the string may be counted as structural characters. Acceptable because `JSON.parse` in the worker will still catch the real error.

---

## Summary

| # | Finding | Severity | Confidence | Status |
|---|---------|----------|------------|--------|
| 1 | Bearing fallback for identical points | Low | High | Verified correct |
| 2 | Silent scene normalization | Low | High | Verified: UI warns |
| 3 | resetSize in finally | Low | High | Verified correct |
| 4 | Fixed degenerate bounds padding | Low | High | Acceptable |
| 5 | Filename sanitization fallback | Low | High | Verified correct |
| 6 | checkJsonDepth unterminated string | Low | Medium | Acceptable |
