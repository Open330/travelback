# Cycle 8 core deep review — 2026-07-24

## Review identity

- Roles: code reviewer, security reviewer, tracer, debugger
- Baseline: `9b3343cd0c01fabb84dc47f4f34c28238d98a99e`
- Method: read-only repository inventory, full production-source inspection, cross-file control/data-flow tracing, test-contract inspection, history review, and deduplication against the existing `.context` corpus

## Outcome

**0 genuinely new, actionable Cycle 8 findings.**

No candidate survived all of the required gates: a reachable production path, a concrete incorrect or unsafe outcome, exact current-code evidence, and deduplication against prior reviews, plans, deferred records, and the Cycle 1–7 remediation history.

## Coverage and traces

### Code correctness and debugging

- Traced every session ingress through `src/components/FileUpload.tsx`, `src/components/JourneyCreator.tsx`, and the sample loader into `src/app/page.tsx`. Checked parse/sample invalidation, export settlement, map-artifact cleanup, playback reset, trim ownership, scene reset, focus publication, and stale-completion guards.
- Traced playback and export state across `src/lib/usePlaybackController.ts`, `src/lib/useExportController.ts`, `src/lib/videoEncoder.ts`, `src/components/ExportPanel.tsx`, `src/components/MapView.tsx`, and `src/lib/map-render.ts`. Checked cancellation leases, generation ownership, object-URL revocation, map resize/idle cleanup, publication order, save/share recovery, and manual-camera restoration.
- Traced trim, scene, and camera edits through `TrackWorkspace`, `TimelineSelector`, `SceneEditor`, `camera`, `interpolate`, `map-geometry`, and `map-export-presentation`. Checked accepted-versus-draft ranges, segment remapping, no-op commits, preview termination, degenerate distance behavior, antimeridian wrapping, and export geometry.
- Traced journey authoring through map-generation changes, waypoint drag terminal paths, confirmation snapshots, cancellation, and completion. The current ownership and cleanup paths did not expose a new stale commit or camera-state loss.
- Inspected all parser and worker production paths in `src/lib/parser.ts`, `src/lib/googleJsonParser.ts`, `src/lib/parse-utils.ts`, and `src/workers/trackParser.worker.ts`, including abort/timeout settlement, structured-clone boundaries, XML lexical guards, Google JSON variants, point/segment bounds, and error translation.

### Security and trust boundaries

- Checked imported-file trust boundaries, parser budgets, XML/JSON handling, worker messages, manufactured names, filename handling, and React rendering sinks. No new injection, traversal, unsafe HTML, or unbounded-input path survived review.
- Checked `src/app/layout.tsx`, `scripts/harden-static-export.mjs`, `scripts/serve-static.mjs`, local map-style assets, and base-path normalization. The current static CSP hardening, local-only map sources, descriptor-backed file serving, symlink containment, response headers, and path checks did not expose a new production vulnerability.
- Checked outbound network surfaces. Production fetches are limited to the same-origin sample asset; guide links are fixed HTTPS destinations with `noopener noreferrer`; map styles have empty source tables and no remote glyph or sprite references.
- Inspected the E2E supervisor and wrappers statically, but did not promote any supervisor hypothesis without the required deterministic pre-fix reproduction and exact survivor evidence.

### Repository-wide inventory

- Inventoried all tracked application, component, library, worker, style, public-asset, script, configuration, workflow, unit-test, process-test, and E2E files before narrowing to review-relevant implementation and contract paths.
- Read all production files under `src/app`, `src/components`, `src/lib`, and `src/workers`, plus `src/types.ts`, build/static-server scripts, Playwright and Next configuration, package scripts, the Pages workflow, and architecture/convention documentation.
- Used unit, process, and E2E test names and focused assertions to verify intended contracts and identify uncovered branches. No browser, server, process-supervisor, full-suite, or E2E execution was performed in this review role.

## Deduplication

The final sweep explicitly excluded already-owned or already-remediated roots, including:

- Cycle 7 bounded parser collection/cardinality work, the 250,000/125,000 parser boundaries, worker regeneration, Unicode filename truncation, and toolbar/journey documentation;
- Cycle 6 GPX route fallback, removal of obsolete wrapped segments, Scene Editor preview lifecycle, and semantic no-op drag/export preservation;
- earlier export settlement and recovery, camera restoration, XML lexical-depth/ownership, renderer wrapping, interpolation, responsive controls, i18n, accessibility, static-serving, CI, and supervisor fixes; and
- the three documented portable-platform supervisor residuals in `deferred-p01-platform-boundaries-cycle2-2026-07-23.md`.

Other inspected hypotheses that mapped to existing records rather than new defects included Mediabunny finalization limits, `preserveDrawingBuffer`, overview viewport heuristics, XML materialization limits, and portable process-identity limitations.

## Safety

This role made no source, plan, generated-asset, dependency, workflow, or deployment changes. It ran no browser/Chrome, E2E, server, supervisor, full test suite, signal, commit, push, or deployment command.
